name: Sync Issues to Notion

on:
  issues:
    types: [opened, edited, closed, reopened, labeled, unlabeled]
  issue_comment:
    types: [created]

permissions:
  issues: read

jobs:
  sync-to-notion:
    runs-on: ubuntu-latest
    # Only run for issue events OR comments that contain time logs
    if: >
      github.event_name == 'issues' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, 'Time:'))
    steps:
      - name: Sync issue to Notion database
        uses: actions/github-script@v7
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
        with:
          script: |
            const issue = context.payload.issue;
            const notionKey = process.env.NOTION_API_KEY;
            const dbId = '30a95da6-c9dc-805a-8dcc-cf1dc2694ea2';

            // Map GitHub issue state to Notion status
            const statusMap = { open: 'Backlog', closed: 'Done' };
            const status = statusMap[issue.state] || 'Backlog';
            const labels = issue.labels.map(l => l.name);

            // Parse time from a string like "1h30m", "45m", "2h", "90m"
            function parseMinutes(str) {
              let total = 0;
              const hours = str.match(/(\d+)\s*h/i);
              const mins = str.match(/(\d+)\s*m/i);
              if (hours) total += parseInt(hours[1]) * 60;
              if (mins) total += parseInt(mins[1]);
              return total;
            }

            // ISO week string e.g. "2026-W09"
            function getISOWeek(dateStr) {
              const d = new Date(dateStr);
              const day = d.getUTCDay() || 7;
              d.setUTCDate(d.getUTCDate() + 4 - day);
              const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
              const week = Math.ceil((((d - yearStart) / 86400000) + 1) / 7);
              return `${d.getUTCFullYear()}-W${String(week).padStart(2, '0')}`;
            }

            // Month string e.g. "2026-03"
            function getMonth(dateStr) {
              const d = new Date(dateStr);
              return `${d.getUTCFullYear()}-${String(d.getUTCMonth() + 1).padStart(2, '0')}`;
            }

            // Fetch all comments and sum up time entries (format: "Time: Xh Ym")
            const commentsRes = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              per_page: 100
            });
            let totalMinutes = 0;
            let totalTokens = 0;
            let totalCost = 0.0;
            for (const comment of commentsRes.data) {
              const timeMatch = comment.body.match(/Time:\s*([\dhmHM\s]+)/);
              if (timeMatch) totalMinutes += parseMinutes(timeMatch[1]);

              const tokensMatch = comment.body.match(/Tokens:\s*(\d+)/);
              if (tokensMatch) totalTokens += parseInt(tokensMatch[1]);

              const costMatch = comment.body.match(/Cost:\s*([\d.]+)/);
              if (costMatch) totalCost += parseFloat(costMatch[1]);
            }

            // Search for existing page with this issue URL
            const searchRes = await fetch(`https://api.notion.com/v1/databases/${dbId}/query`, {
              method: 'POST',
              headers: {
                'Authorization': `Bearer ${notionKey}`,
                'Content-Type': 'application/json',
                'Notion-Version': '2022-06-28'
              },
              body: JSON.stringify({
                filter: {
                  property: 'GitHub URL',
                  url: { equals: issue.html_url }
                }
              })
            });
            const searchData = await searchRes.json();
            const existingPage = searchData.results && searchData.results[0];

            const properties = {
              'Title': {
                title: [{ text: { content: `#${issue.number} ${issue.title}` } }]
              },
              'Status': {
                select: { name: status }
              },
              'Labels': {
                multi_select: labels.map(l => ({ name: l }))
              },
              'GitHub URL': {
                url: issue.html_url
              },
              'Created': {
                date: { start: issue.created_at }
              },
              'Project': {
                select: { name: 'TIL' }
              },
              'Source': {
                select: { name: 'Claude Code' }
              },
              'Time Spent (min)': {
                number: totalMinutes
              },
              'Tokens': {
                number: totalTokens > 0 ? totalTokens : null
              },
              'API Equiv ($)': {
                number: totalCost > 0 ? Math.round(totalCost * 100) / 100 : null
              },
              'Week': {
                select: { name: getISOWeek(issue.created_at) }
              },
              'Month': {
                select: { name: getMonth(issue.created_at) }
              }
            };

            if (existingPage) {
              const updateRes = await fetch(`https://api.notion.com/v1/pages/${existingPage.id}`, {
                method: 'PATCH',
                headers: {
                  'Authorization': `Bearer ${notionKey}`,
                  'Content-Type': 'application/json',
                  'Notion-Version': '2022-06-28'
                },
                body: JSON.stringify({ properties })
              });
              const updateData = await updateRes.json();
              if (updateData.id) {
                console.log(`Updated Notion page for issue #${issue.number} (${totalMinutes} min, ${totalTokens} tokens, $${totalCost.toFixed(2)})`);
              } else {
                console.log('Update response:', JSON.stringify(updateData));
              }
            } else {
              const createRes = await fetch('https://api.notion.com/v1/pages', {
                method: 'POST',
                headers: {
                  'Authorization': `Bearer ${notionKey}`,
                  'Content-Type': 'application/json',
                  'Notion-Version': '2022-06-28'
                },
                body: JSON.stringify({
                  parent: { database_id: dbId },
                  properties
                })
              });
              const createData = await createRes.json();
              if (createData.id) {
                console.log(`Created Notion page for issue #${issue.number} (${totalMinutes} min, ${totalTokens} tokens, $${totalCost.toFixed(2)})`);
              } else {
                console.log('Create response:', JSON.stringify(createData));
              }
            }
